/**
 * App settings.
 * You can inject these constants as a service in angular,
 * import this module directly otherwise.
 *
 * @author centsent
 *
 */
const AppSettings = {
  // angular app module name.
  appName: 'hydeo',
  // Media state.
  mediaState: {
    PLAY: 'play',
    PAUSE: 'pause',
    STOP: 'stop'
  }
};

export default AppSettings;
